/// <reference path="../../components/DefinitelyTyped/requirejs/require" />

import app = require('../app');
import config = require('../config');

var controller = app.controller('InspectorController', ['$scope', 'config', function ($scope, config) {

}]);

export = controller;
